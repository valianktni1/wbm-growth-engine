# V1.4.1 — Compatible with the corrected Booking workspace

Retains all pending V1.4.0 features: default 12-month planner, 18/24-month and custom ranges, yearly targets, recorded enquiry conversion, venue campaign materials and on-demand weekly actions.

Pairs with Booking V8.43.1, merged from the user-supplied corrected V8.42.1 Fast Email Workspace. It preserves that release's dashboard and mailbox performance repairs. Supersedes the held Growth V1.4.0 / Booking V8.42 paired archive.

No feature or database schema changes from the pending Growth V1.4.0 code. Version/error guidance and the paired deployment script are updated for the corrected Booking base. No passwords, configuration or sending permissions are changed.

43 automated checks passed: 20 Growth and 23 focused Booking checks. The corrected mail/dashboard/mobile implementation is retained; new timestamp evidence remains within the batched query path. Shell/JavaScript syntax and Python compilation checks passed. Docker and real-browser checks remain for deployment; the script validates builds, health, a 732-day availability request and yearly totals on TrueNAS.
